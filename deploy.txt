https://github.com/nodesource/distributions/blob/master/README.md
?fbclid=IwAR2s0B6DctqAp2QGmmvIdhygHv8LgVkW0l_X6cdUOI2Be9YbxRCOCnXaWvo

UP GITHUP
git init
git add .
git commit -m "abcxyz"
git brach main
git remote add origin (link git)
git push -u origin main

EC2
cai nodejs :
Node.js v14.x

# As root
curl -sL https://rpm.nodesource.com/setup_14.x | bash -

# No root privileges 
curl -sL https://rpm.nodesource.com/setup_14.x | sudo bash -

cai github:
sudo yum install git


mkdir (ten thu muc)
ls: xem tap tin trong thu muc hien tai
cd (ten thu muc)
ls
git clone (link git)
ls
nano (aws.js hoac cai loz gi do)

node app.js (index.js neu k doi ten)